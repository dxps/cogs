[package]
name = "cogs_shared"
description = "the shared domain and dtos between the svc and ui"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "=1.0.219", features = ["derive"] }
thiserror = { version="=2.0.12" }
anyhow = { version="=1.0.98" }

[lints]
workspace = true
